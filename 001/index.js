const name = 'Eli';
const age = 12;
const checked = true;
const nothing = undefined;
const nullable = null;
const vector = [1, 2, 3];
const obj = { 
    game: 'Assassin\'s Creed', 
    year: 2018, 
    consoles: ['PS4', 'XONE'],
};


function circleArea(diameter){
    return Math.PI * (diameter * diameter) / 4;
}

const result = circleArea(10);
console.log(result);

let hour = 10;

// if (hour >= 6 && hour <12){
//     console.log('Bom dia')
// }
// else if (hour >=12 && ) {

// }
// else if () {

// }
// else


// area= pi*d2/4
// perimetro = pi*d = 2*pi*r